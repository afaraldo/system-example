import invalidateSession from 'frontend/utils/invalidate-session';
import { module, test } from 'qunit';

module('Unit | Utility | invalidateSession', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = invalidateSession();
    assert.ok(result);
  });
});

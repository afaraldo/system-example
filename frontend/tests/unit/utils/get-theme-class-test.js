import getThemeClass from 'frontend/utils/get-theme-class';
import { module, test } from 'qunit';

module('Unit | Utility | getThemeClass', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getThemeClass();
    assert.ok(result);
  });
});

<h1>
  Inscripciones
  <LinkTo @route="registrations.new" class="btn btn-primary">Inscribir</LinkTo>
</h1>
{{models-table-server-paginated
        data=model
        columns=columns
        useFilteringByColumns= false
        useNumericPagination= true
        showCurrentPageNumberSelect= false
}}
{{outlet}}